import ProductLayout from '../../components/products/ProductLayout'
import { productPages } from '../../data/productPages'

export default function KadaLedger() {
  return <ProductLayout config={productPages['kada-ledger']} />
}
